# PMS-RS AI Documentation

The canonical AI/contributor workflow contract now lives in `docs/workflow`.

Start with:

- `AGENTS.md`
- `docs/workflow/README.md`

This directory is retained only as a historical pointer.
